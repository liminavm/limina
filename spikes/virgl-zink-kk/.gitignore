# build artifacts — source + RESULTS/probe-output kept, binaries/logs not
eglprobe
eglprobe.dSYM/
glprobe
glprobe.dSYM/
vrendprobe
vrendprobe.dSYM/
# transient build/boot logs (never kept — RESULTS.md captures what matters)
*.log
