# libkrun — patch-audit ledger

126 patches audited (at base `07a3f40973ce`). Schema + protocol: `README.md`.
Rows are keyed by SUBJECT; ordinals refer to the retired `patches/libkrun/` series.

**FORK MODEL since 2026-08-06 (task #14):** the series was rebased onto upstream tip
`07fd40dc` (upstream moved to github.com/libkrun/libkrun) and lives as
`liminavm/libkrun` branch `limina` (124 commits; pinned by `third_party/manifest.toml`).
Dropped as upstream-superseded: 0038 (blk serial = block_id, upstream df85b8b),
0044 (blob-map overflow guard, upstream #790 checked_blob_map_addr — upstream also ships
an equivalent regression test, so 0071's copy of ours was dropped too), 0090 (fs worker,
upstream 4e01b23). 0020 was squashed into 0017. Notable rebase convergences: upstream
grew its own live vCPU pause/resume (VmCtl) at exactly our M9 quiesce boundary — our
`pause_vcpus`/`resume_parked_vcpus` now ride it (vtimer hold included) and `handle_pause`
is gone; upstream's display/EDID consolidation (`crate::display`) absorbed our DisplayInfo
EDID field, our 0119/0120 additions (identity/range/timings/`connected`) layer on top;
consoles are explicit-only upstream now (`disable_implicit_console` no longer exists).

**RESOLVED (2026-08-06) — the "cold-boot wedge" root cause:** NOT a timer/vtimer bug and NOT
in libkrun at all. A guest-memory autopsy of a wedged boot (pause the vCPU at t=100s, read
PC/registers, dump the stack + the PE image at the spinning PC) proved the guest was parked in
EDK2's `CpuDeadLoop()` inside **DxeCore** of the Homebrew-krunkit `KRUN_EFI.silent.fd` — a
**DEBUG_GCC5 build** (live ASSERTs) of slp/edk2@krun-support. The chain, each link read out of
guest RAM: no uart node in the FDT → DxeCore's `SerialPortInitialize` constructor
(FdtPL011SerialPortLib) returns EFI_NOT_FOUND → the generated `ASSERT_EFI_ERROR` in
DxeMain/AutoGen.c(516) fires → DebugAssert formats "ASSERT [DxeCore] …" into the missing
serial port → CpuDeadLoop (100% CPU, zero VM exits, IRQs masked, vtimer disabled — the exact
sampled signature). The trigger became reachable only when upstream removed the implicit
console (ce4146d): a PL011-less FDT never existed before, so the DEBUG firmware's fragility
was unobservable. limina's fix (in the migration commit): `console::attach_dropped` always
attaches an output-dropped PL011 when no console is requested. The "load-sensitive net-test
residue" was NOT a second bug and is unreproducible on the fixed stack: four full suites on
2026-08-06 (incl. one run with a synthetic 100%-CPU core-pinner for the whole duration, a
harsher condition than the dead-looping guest) were all 82/82 with net tests at normal speed
(18–21s — the spinner did not slow them at all, so "one wedged guest's load" alone does NOT
explain the pre-fix timeouts either). Best remaining explanation for the pre-fix 1–3 net
DHCP timeouts: uncontrolled concurrent host activity during those overnight runs — including
one documented mid-suite `cargo build` (all cores) — on top of the wedged guest; recorded as
reconstruction, not measurement (no failing net worker was ever sampled directly). The
earlier "probing masks it / lost-timer-IRQ" reading was misattribution. Upstream-report candidates
(queued, not yet filed): krunkit ships a DEBUG-built silent firmware that dead-loops on a
consoleless FDT (report to slp — RELEASE would degrade gracefully); note our GOP firmware
builds RELEASE by default and carries the same-class CoreRaiseTpl fix at the source.

**FIXED (task #21, 2026-08-06, fork rev `aeafaf23` — folded into the PSCI CPU_ON commit
`5a44abc` at the 2026-08-12 branch fixup-squash; the old rev stays reachable via the
`pre-fixup-2026-08-12` tag):** the pre-SMP pause/snapshot deadlock
found during the autopsy — secondaries parked in the *initial* `boot_receiver.recv()` never
saw Pause/Snapshot events, so `pause()`/`snapshot_vcpus()` waited forever holding the vmm
mutex (reachable by a user suspend during early boot, or any `maxcpus=`-capped guest). The
boot wait now services events with the same select shape as `handle_offline`. RED-first
regression test: `l1_snapshot_completes_with_a_never_onlined_vcpu` (cpus=2 + `maxcpus=1`).
Restore fidelity for not-online vCPUs remains the task #41 limitation (save side only).
Upstream note: the deadlock is an interaction of upstream's krun_vm_pause with our carried
PSCI CPU_ON boot-channel park (0094-era), so the fix travels with that series.

**2026-08-12 fixup-squash note:** the branch was rewritten (137 → 124 commits, tree-identical;
old tip reachable via the fork tag `pre-fixup-2026-08-12`). Rows below whose commit was folded
now describe a delta living *inside* its parent commit rather than a standalone commit:
0075/0077/0078 → 0074; the CreateBlob vkr_seq epoch fix → 0079; the balloon-reporting fixture,
EDID test-import, PSCI pause/snapshot-wait, xHCI abort-doorbell, force-OFF poll, fence-wedge,
drain-then-classify, leaked-contexts, cursor-alpha, coexist attach/detach and vsock-teardown
fixes → their respective parent feature commits. Three composites were split first; the split-out
non-fix halves (FLUSHDBG diagnostics, xHCI snapshot-test enrichment, eventfd-drain/mmio
un-negotiate) remain standalone rows-to-be.

| ord | subject | files | diag | need | checked | issue | mr | sec | fold | tier | disp | notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 0001 | limina: software 2D virtio-gpu scanout for GL-less hosts (no renderer init) | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` +2 |  | yes — tip worker.rs:129 still routes RESOURCE_CREATE_2D → resource_create_3d; rutabaga is not Option, no renderer-less path | main @c652b56 2026-08-03 | adjacent #426 (fallback), #740 (gfxstream macOS) | none direct | no | standalone (base of coexist) | host (macOS/GL-less) | CARRY (macOS-shaped); the renderer-less-2D MECHANISM could interest krunkit GL-less hosts | gap is real upstream but upstream's macOS answer is gfxstream, not sw-2D |
| 0002 | limina: PL011 drops serial output on WouldBlock instead of erroring | `src/devices/src/legacy/aarch64/serial.rs` |  | yes — bug present (serial.rs:248, no WouldBlock arm; libkrun's own PL011, not Firecracker's 16550) | libkrun/libkrun@main 2026-08-03 (raw source) | none found | none | no | standalone | host | send (discuss) — drop-on-WouldBlock is the minimal correct choice; frame as "serial output lossy when nothing drains it" (QEMU chardev parity) | expect a maintainer to ask bounded-buffer-vs-drop |
| 0003 | limina: virtio-console ConsoleInOut port + quiet raw-mode ENOTTY | `src/vmm/src/builder.rs`, `src/vmm/src/resources.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Test-harness ConsoleInOut console-port shape + ENOTTY demotion; config surface upstream wouldn't take |
| 0004 | limina: HVF support 16-bit (halfword) MMIO writes | `src/hvf/src/lib.rs` |  | yes — bug present (tip write-match `_ => panic!("unsupported mmio len")` hvf_lib.rs:664; read-match already has 2=>) | upstream main HEAD 2026-08-03 (raw file reads) | none | none — clean 1-liner | low (guest halfword MMIO store → vCPU-thread panic/abort) | standalone | host | **PR candidate** (add u16 write arm) | drop the "limina:" subject prefix |
| 0005 | limina: FDT mark PL011 serial node as arm,primecell (real ttyAMA0 tty) | `src/devices/src/fdt/aarch64.rs` |  | yes — bug present (fdt/aarch64.rs:346 still "arm,pl011" only; RTC/GPIO already carry arm,primecell) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | no | **HARD dep on 0004** (submit together or the amba probe's first writew re-panics) | host | send with 0004 | consistency fix |
| 0006 | limina: virtio-mmio default ready queue to max_size when QueueNum unset | `src/devices/src/virtio/mmio.rs` |  | yes — bug present (mmio.rs:472 unchanged) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | low (relaxes spec enforcement — a compliant driver programs QueueNum first) | standalone | host | discuss upstream — QEMU-parity for a real EDK2 VirtioGpuDxe spec violation; propose and let slp decide policy | has a regression test |
| 0007 | limina: virtio-gpu stop the worker thread on device reset | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | fold — later worker-stop fixup of the reset lifecycle; README squash plan is 0007+0022+0035→one | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0022** (persist renderer across reset) | host | fold | the worker-thread-stop half of the reset lifecycle; rides 0022's CARRY disp |
| 0008 | limina: virtio-gpu hardware cursor (service the cursor queue) | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` +3 |  | yes — CONFIRMED PANIC at tip (worker.rs:191 UpdateCursor, :194 MoveCursor; cursor queue dropped device.rs:214) | main @c652b56 2026-08-03 | none | none | yes:guest-triggerable abort | standalone (0015 depends on it) | host | **UPSTREAMABLE — the depanic + queue service is generic** (vtable ext macOS-flavored but optional) | any guest issuing UPDATE_CURSOR aborts the VMM at tip; no upstream issue tracks it |
| 0009 | limina: log renderer-init failure instead of swallowing it | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Log taste (renderer-init error text); re-offer only if a GPU MR opens |
| 0010 | limina: virtio-gpu coexist — route fences by ring + graceful renderer fallback | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | yes — CONFIRMED abort at tip: virtio_gpu.rs:334 .expect("Fallback rutabaga initialization failed") on the GL-less case | main @c652b56 2026-08-03 | **#426 (closed PR "auto-fallback to safe rutabaga config")** — upstream's own take; ours supersedes with degrade-to-None | none | SEC-adjacent (init-time abort) | tied to 0001 | host (macOS/coexist) | CARRY (ring-routing+None-degrade tied to 0001); the **don't-.expect()-panic half is generically upstreamable** | no upstream notion of mixed venus/2D contexts |
| 0011 | limina: log hv_vm_map failures with alignment breakdown | `src/hvf/src/lib.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Log taste; hv_vm_map alignment breakdown — the diagnostic that found the 16k/4k mismatch |
| 0012 | limina: virtio-gpu map blob via map_ptr for upstream virglrenderer 1.3.0 | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | yes — delta live (tip still gates map_ptr on handle_type==APPLE, vgpu.rs:913) | upstream main HEAD 2026-08-03 (raw file reads) | none | keep-downstream (fork-coupled) | **none** — in our tree the broadened path flows through upstream's own checked_blob_map_addr, no overflow reachable | standalone | host | downstream; discuss upstream only if libkrun moves macOS off APPLE-blob virglrenderer | DISCLOSURE GATE CLEARED — not a security patch; compat/enablement tied to our virgl fork |
| 0013 | gpu(macos): implement SET_SCANOUT_BLOB as zero-copy IOSurface present | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs`, `src/display/libkrun_display.h` +7 |  | yes — CONFIRMED PANIC at tip (worker.rs:335 SetScanoutBlob unimplemented) | main @c652b56 2026-08-03 | #740 (open, macOS blob via gfxstream — different backend) | none | yes:guest/mutter first page-flip aborts VMM | standalone | host (macOS/venus) | CARRY (IOSurface+virgl-fork exports); the **depanic→ErrUnspec floor is generically upstreamable** | upstream macOS GPU = gfxstream/MoltenVK, not venus/virglrenderer |
| 0014 | limina #30: don't panic the GPU worker on scanout readback failure | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | yes — bug present (tip read_2d_resource .transfer_read().unwrap(), vgpu.rs:512) | upstream main HEAD 2026-08-03 (raw file reads) | none | good (propagate err; flush_resource already handles it) | med (blob/venus scanout readback → GPU-worker panic → guest wedges) | standalone (sibling of 0024) | host | **PR candidate** | pair with 0024 |
| 0015 | limina: treat cursor resource pixels as alpha-carrying despite XRGB dumb format | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | yes — depends on 0008 (cursor path absent upstream) | main @c652b56 2026-08-03 | none | none | no | rides 0008 | host | UPSTREAMABLE-with-0008 (QEMU precedent: cursor de-facto ARGB) | small correctness follow-on; no upstream discussion found |
| 0016 | gpu: log context lifecycle and error responses at visible levels | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Log taste; ctx lifecycle + RESP_ERR at visible levels |
| 0017 | limina: fence-accurate scanout presents (LIMINA_FENCE_PRESENT, #8/#31) | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` | DIAG | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | strip-diag-first | host | carry | /tmp/limina-* marker + LIMINA_FENCE_PRESENT/LATCH_MS env; needs a real config API before any upstream shape |
| 0018 | limina: hold guest flush fences until present + latch (#8 half 2 host side) | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Present-pacing policy; pairs patches/linux/0001 guest fence — Wave-3 RFC at best, not a fix |
| 0019 | limina: complete held flush fences on supervisor shown-acks (#8 latency collapse) | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | README verbatim: stays downstream — limina supervisor protocol (LIMINA_SHOWN_ACK_FD) |
| 0020 | gpu: demote per-frame FLUSHDBG/SET_SCANOUT_BLOB lines to debug | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | fold — log-level fixup (demote per-frame FLUSHDBG/SET_SCANOUT_BLOB to debug) | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0013/0017** (the present/scanout patches whose logs it demotes) | host | fold | pure log-noise reduction; rides the parents (carry) |
| 0021 | gpu: fence-present must never wedge the guest scanout pipeline | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | fold — correctness follow-up: fence-present must never wedge the scanout pipeline | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0017** (fence-accurate presents) | host | fold | belongs in 0017's body; carry (0017 is DIAG/policy, not upstream-shaped) |
| 0022 | limina: virtio-gpu persist the renderer across device reset (singleton fix) | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | genuine bug, latent upstream — upstream device has NO reset(); activate() re-runs worker.run() detaching the thread; virgl_renderer_init singleton re-init bug real but UNREACHED (upstream never orderly-resets) | main @c652b56 2026-08-03 | none | none | no | **absorbs 0007+0035** (README squash plan) | host | CARRY (built on limina's reset() machinery upstream lacks); the singleton limitation is worth an upstream note | upstreaming needs limina's whole reset-lifecycle first |
| 0023 | limina: distinguish guest reboot (PSCI SYSTEM_RESET) from power-off | `src/hvf/src/lib.rs`, `src/vmm/src/lib.rs`, `src/vmm/src/macos/vstate.rs` |  | yes — bug present (tip SYSTEM_RESET => Ok(Shutdown), hvf_lib.rs:562, collapsed with SYSTEM_OFF) | upstream main HEAD 2026-08-03 (raw file reads) | none | needs rework (adds FC_EXIT_CODE_REBOOT=125, supervisor-coupled) | none (correctness/lifecycle) | standalone | host | upstream-worthy (krunkit needs it too) — propose the mechanism, let slp pick the exit code | carries limina exit-code contract |
| 0024 | limina: implement virtio-gpu transfer_read (TRANSFER_FROM_HOST_3D) | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | yes — bug present (tip transfer_read body = panic!("unimplemented"), vgpu.rs:593) | upstream main HEAD 2026-08-03 (raw file reads) | none | good (delegate to rutabaga like transfer_write) | med (stock-4k coexist/vrend: any glReadPixels/glxinfo/WebGL → GPU-worker panic → hang) | standalone (pair 0014) | host | **PR candidate** | our impl also short-circuits sw2d resources |
| 0025 | limina: virtio-gpu runtime display resize (config-change mechanism) | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` | DIAG | yes — upstream has NO runtime-resize path: displays immutable, read_config hardcodes events_read:0, write_config logs+ignores, no VIRTIO_GPU_EVENT_DISPLAY signalling | main @c652b56 2026-08-03 [LIMINA-NEW mechanism] | none | none | no | **strip-diag-first** | host | upstream-after-cleanup — mechanism is virtio-spec-shaped (events_read/clear + try_signal_config_change), NOT embedder-coupled; krunkit windowing would want it | carries /tmp/limina-readback-delay + /tmp/limina-dump-staging + per-present readback DIAG hooks — strip first |
| 0026 | limina: expose the GPU DisplayResizeHandle to the host VMM | `src/devices/src/virtio/gpu/mod.rs`, `src/vmm/src/builder.rs`, `src/vmm/src/lib.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Embedder plumbing: Vmm::gpu_resize_handle() for limina-vmm; chained to 0025 |
| 0027 | limina: virtio-gpu present the scanout rect at the resource's stride (de-shear) | `src/devices/src/virtio/gpu/virtio_gpu.rs` | DIAG | yes — real correctness; upstream read_2d_resource/flush copies as a flat blob at resource.width stride → shears when resource wider than scanout | main @c652b56 2026-08-03 | none | none | no | standalone | host | **UPSTREAMABLE** — pure bug, unit-tested | **strip the /tmp/limina-readback-delay DIAG hook first** (guest-independent debug) |
| 0028 | limina: HVF — don't panic the worker on unhandled guest traps | `src/hvf/src/lib.rs`, `src/vmm/src/macos/vstate.rs` |  | **yes — bug present, NOT converted since base**: tip handle_psci_request still ends `val => panic!` (hvf_lib.rs:570); 4 sibling panics (mmio len 585/664, exit 618, sysreg 702/718, EC 757) all live | upstream main HEAD 2026-08-03 (raw file reads) | #577 open (same class, virtio-net); slp's #418 (expect→retry) shows upstream receptive | good (NOT_SUPPORTED for unknown PSCI/SMC; Error::Unhandled → clean teardown) | **high (one guest smc/unknown-HVC aborts stock upstream VMM — SIGABRT DoS; breaks the stock-compat floor)** | standalone | host | **strong upstream PR — FLAGSHIP / umbrella**; frame as DoS hardening, cite #577 | verified guest-triggerable on tip; send FIRST — neutralizes the whole panic surface incl. 0094's |
| 0029 | limina: virtio-gpu advertise only the capsets the renderer backs | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | yes — CONFIRMED: device.rs:166 num_capsets:5 hardcoded vs capset_mask=0 registering all 9 | main @c652b56 2026-08-03 | **#565 OPEN ISSUE — exact match** (spun out of PR #560 review) | PR #560 (open, migrating rutabaga w/ capset_mask API) | no | standalone | host | **UPSTREAMABLE / directly wanted — strongest in the cluster**; attach to #565, aligns with in-flight #560 | note our helper vs upstream's "query Rutabaga" framing |
| 0030 | limina: rutabaga tolerate coexist 2D-resource attach/detach on a 3D context | `src/rutabaga_gfx/src/rutabaga_core.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | yes:error-masking(low) | standalone | host | carry | 07-04 B2 #6: unconditional Ok(()) on missing ctx/resource; upstream would want it coexist-gated |
| 0031 | limina: map KRUN_DISPLAY_ERR_METHOD_UNSUPPORTED (-2) in into_rust_result! | `src/display/src/c_to_rust.rs` |  | yes — tip c_to_rust.rs into_rust_result! has -1/-3/-4 arms, NO -2; MethodNotSupported falls to catch-all=InternalError | main @c652b56 2026-08-03 [SHARED-BUG] | none | none | no | standalone | host | **UPSTREAM-NOW** — trivial 5-line enum arm, unaffected by the #319 display refactor; cleanest send-now | 07-04 already flagged "trivial; send first" |
| 0032 | limina: virtio-gpu read the venus scanout IOSurface for the headless capture sink | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/rutabaga_gfx/src/generated/virgl_renderer_bindings.rs`, `src/rutabaga_gfx/src/rutabaga_core.rs` +1 | DIAG | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | strip-diag-first | host | carry | Headless capture sink + fork-only virgl_renderer_resource_read_iosurface |
| 0033 | limina: balloon — reclaim free pages with MADV_FREE_REUSABLE + 16 KiB-safe coalescing | `src/devices/src/virtio/balloon/device.rs` |  | yes — tip process_frq still MADV_DONTNEED/MADV_FREE | main tip + #794 head 571caabb 2026-08-03 | none | #794 (competing, OPEN); #703 MADV_FREE (merged) = upstream's current reclaim | no | PARTIAL — the MADV_FREE→MADV_FREE_REUSABLE switch is converging (#794's own fallback makes it too); the 16k coalescer is ours | host | **CARRY + candidate-upstream (the madvise switch)** | maintainer thread favors us: slp "not convinced we need [#794]" + flags its VM-exit-per-refault tax; our MADV_FREE_REUSABLE debits phys_footprint WHILE MAPPED (spike-verified ~1GiB drop) so no unmap/refault needed, and satisfies the Jetsam concern #794 cites. Upstream the madvise switch; offer the ReclaimCoalescer as a 16k-correctness add-on; re-confirm on macOS bumps |
| 0034 | limina: balloon — inflate/deflate, target/actual, DEFLATE_ON_OOM, BalloonControlHandle | `src/devices/src/virtio/balloon/device.rs`, `src/devices/src/virtio/balloon/event_handler.rs`, `src/devices/src/virtio/balloon/mod.rs` +3 |  | yes — tip has classic FRQ+stats only, NO host-driven target/inflate/deflate handler bodies | main tip + #794 2026-08-03 | #707 (deflate-on-oom, tangential) | none competing — #794 does NOT touch inflate/deflate/target | no (guest-driven actual/PFN parse, bounds already handled) | no upstream equivalent | host | **CARRY (limina-specific control plane)** — composes with #794, no collision | BalloonControlHandle/target/process_ifq/process_dfq is purely additive/orthogonal; direction of travel per slp/mtjhrc = a virtio-balloon 2.x API property, not our handle shape |
| 0035 | limina: virtio-gpu — drop leaked contexts/resources on a dirty device reset | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/rutabaga_gfx/src/rutabaga_core.rs` |  | yes — hardening; same as 0022 (no upstream reset_session_state; leaked-id collision unreached because upstream never resets) | main @c652b56 2026-08-03 | none | none | yes:id-collision → InvalidContextId cascade, UAF-adjacent | **folds with 0022** | host | CARRY-with-0022 | real field crash (gdm/nautilus cascade) |
| 0036 | limina: virtio-gpu — demote per-frame present DIAGs (FLUSH2/FENCEPRESENT) to trace | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | fold — DIAG demotion (FLUSH2/FENCEPRESENT per-frame → trace) | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0017/0027** | host | fold | log-taste fixup of the present + de-shear patches; carry |
| 0037 | limina: virtio-input — return to Inactive on reset() so the device can re-activate | `src/devices/src/virtio/input/device.rs` |  | yes — bug present (input/device.rs:250, no device_state=Inactive on reset) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | no | family w/0007 (GPU reset-lifecycle) | host | **send-now** — clean one-liner |  |
| 0038 | limina: virtio-blk serial = block_id (stable /dev/disk/by-id/virtio-<id>) | `src/devices/src/virtio/block/device.rs` |  | **superseded-upstream@df85b8b** — build_disk_image_id now prefers caller block_id (folded into the existing fn, semantically identical) | libkrun/libkrun@main 2026-08-03 (raw source) | n/a | df85b8b (Ryan Dunn, 2026-07-08) | no | drop on rebase | host | **DROP — already upstream** |  |
| 0039 | limina: virtio-input worker blocks (epoll -1) instead of a pointless 1s timeout | `src/devices/src/virtio/input/worker.rs` |  | yes — bug present (input/worker.rs:121 still epoll timeout 1000) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | no | standalone | host | **send-now** — pure idle-wakeup removal |  |
| 0040 | limina: hvf vtimer — multiply before dividing so the WFI timeout isn't ~1.6% short | `src/hvf/src/lib.rs` |  | yes — bug present (tip Duration::from_nanos((cval-now)*(1e9/cntfrq)) divide-before-multiply, hvf_lib.rs:749) | upstream main HEAD 2026-08-03 (raw file reads) | none | clean (u128 multiply-first) | none (idle-wakeup/perf: ~1.6% short WFI) | standalone | host | **PR candidate** — trivially correct |  |
| 0041 | rutabaga: balance the eager macOS blob map on unref_resource | `src/rutabaga_gfx/src/virgl_renderer.rs` |  | yes — bug present (tip unref_resource calls only virgl_renderer_resource_unref, no macOS unmap, rutabaga.rs:548) | upstream main HEAD 2026-08-03 (raw file reads) | none filed | good (add #[cfg(macos)] resource_unmap before unref) | low (macOS venus address-space leak → eventual ENOMEM session collapse) | standalone | host | **PR candidate** — cross-repo pairing with our virgl fork map behavior | pairs virgl 0022 |
| 0042 | virtio-i2c: new device with an emulated SBS smart battery slave | `src/devices/src/fdt/aarch64.rs`, `src/devices/src/virtio/i2c/device.rs`, `src/devices/src/virtio/i2c/event_handler.rs` +4 |  | LIMINA-NEW — upstream has NO virtio-i2c at all (src/devices/src/virtio/ has no i2c/) | main @c652b56 2026-08-03 [LIMINA-NEW] | none | none | yes:guest-triggerable virtqueue parsing (SMBus word/block read handler) — bounds-review before any upstream | standalone | host (guest = stock i2c-virtio + sbs-battery) | CARRY — mechanism (virtio-i2c dev ID 34) is upstreamable but the CoreFoundation/macOS battery source is limina policy; propose only if upstream wants a generic i2c | read-only register file, NAKs unknowns; attached only when host supplies a battery_provider |
| 0043 | limina: hvf — round map/unmap sizes up to the host page granule | `src/hvf/src/lib.rs` |  | yes — delta live (tip map_memory/unmap_memory pass size straight to hv_vm_map, hvf_lib.rs:275/290; no rounding) | upstream main HEAD 2026-08-03 (raw file reads) + BLOB_ALIGNMENT cross-check DONE | none | needs rework (hardcodes host getpagesize) | low (4k-guest sub-host-page blob map → HV_BAD_ARGUMENT → venus unusable on stock 4k) | standalone | host | upstream-worthy as generic "round map/unmap to host granule"; NOT superseded | CHECKED: upstream libkrun has NO negotiated host-page granule; VIRTIO_GPU_F_BLOB_ALIGNMENT is guest-kernel-side, does not replace our host-side round-up |
| 0044 | limina: virtio-gpu — reject blob-map offset/size overflow | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | **superseded-upstream@#790** — upstream checked_blob_map_addr (vgpu.rs:1027, checked_add) guards all 3 resource_map_blob paths; our unchecked arithmetic is gone from tip | upstream main HEAD 2026-08-03 (raw file reads) | #790 (closed/merged, BinBin — independent third party) | **n/a — already fixed publicly, nothing to file, DISCLOSURE GATE CLEARED** | n/a — already fixed upstream (was provisionally embargo-class; resolved NOT exploitable on tip) | drop/fold on rebase (align to upstream arg order, delete our copy) | host | **drop — superseded by #790** | libkrun handles this DoS class publicly (#577 open); no embargo was needed |
| 0045 | limina: balloon — skip out-of-range FRQ descriptors instead of panicking | `src/devices/src/virtio/balloon/device.rs` |  | fold — robustness fixup: skip out-of-range balloon FRQ descriptors instead of panicking | n/a (fold pass 2026-08-03) | n/a | n/a | yes:balloon FRQ unwrap (guest-triggerable abort) — rides into 0033 | **fold-into:0033** (balloon reclaim) | host | fold | the panic-guard must ride along IF 0033's madvise switch is upstreamed — keep the hardening in the same commit |
| 0046 | rutabaga_gfx: route virgl logs through the level-aware callback | `src/rutabaga_gfx/src/generated/virgl_debug_callback_bindings.rs`, `src/rutabaga_gfx/src/virgl_renderer.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Log taste; binds virgl_set_log_callback for level-aware virgl logs |
| 0047 | virtio/snd: add native in-VMM virtio-snd device (playback enumeration) | `src/devices/Cargo.toml`, `src/devices/src/virtio/mod.rs`, `src/devices/src/virtio/snd/device.rs` +6 |  | LIMINA-NEW — upstream has NO virtio-snd (only Linux-host vhost_user); PR #749 REMOVED the legacy-SND doc mention → upstream moving AWAY from sound | main @c652b56 2026-08-03 [LIMINA-NEW] | none | **PR #749** (coordination only — legacy SND mention removed 2026-06-24; NOT a superseding device) | yes:guest-triggerable virtqueue parsing (control/tx queue handshake, guest-controlled lengths) | **fold-into:0048** (0047 null sink is a throwaway intermediate replaced whole by 0048) | host (guest = stock virtio_snd) | CARRY — native in-VMM virtio-snd (dev ID 25), playback enum + control handshake; different shape from upstream vhost-user, not superseded | gated behind snd feature, modelled on in-tree rng; clean naming |
| 0048 | virtio/snd: CoreAudio output sink with completion pacing | `src/devices/src/virtio/snd/audio_macos.rs`, `src/devices/src/virtio/snd/device.rs`, `src/devices/src/virtio/snd/event_handler.rs` +1 |  | LIMINA-NEW — the real CoreAudio backend for the 0047 device | main @c652b56 2026-08-03 [LIMINA-NEW] | none | none | yes:guest-triggerable virtqueue parsing (tx descriptor pacing, guest PCM frame copy through SPSC ring) | **head commit — absorbs 0047** into "virtio-snd: native in-VMM device (CoreAudio output)" | host | CARRY — correctness core (fixes the ALSA underrun/-EIO race); macOS-cfg only, degrades to null sink on CoreAudio init failure | CoreAudio AUHAL output sink + tx-completion pacing (hw_ptr tracks host DAC) |
| 0049 | virtio/snd: mic capture — opt-in input stream (macOS CoreAudio) | `src/devices/src/virtio/snd/audio_macos.rs`, `src/devices/src/virtio/snd/device.rs`, `src/devices/src/virtio/snd/event_handler.rs` +4 |  | LIMINA-NEW — opt-in mic capture stream on the same device | main @c652b56 2026-08-03 [LIMINA-NEW] | none | none | yes:PRIVACY (host mic → guest) + guest-triggerable rx virtqueue parsing; default-OFF (VmResources::snd_capture) is the mitigation | **fold-into:0048** as a separable follow-on commit (keep separate — it is the opt-in privacy feature) | host (guest = stock virtio_snd capture) | CARRY — INPUT stream 1 + AUHAL input, TCC-prompt on PREPARE, silence-on-deny | release-path flush fixes the 2nd-open wedge; clean naming |
| 0050 | limina: real vCPU pause/quiesce mechanism (macOS/HVF) for M9 snapshots | `src/devices/src/legacy/vcpu.rs`, `src/vmm/src/builder.rs`, `src/vmm/src/lib.rs` +1 |  | yes | #762 diff (PR file-touch) 2026-08-03 + #767 | #748 | overlaps #762 | no | with 0051-0053 | host | **redundant** — #762 quiesces at the same boundary (hv_vcpus_exit at an inter-hv_vcpu_run point, #767 primitive) | same mechanism as our top-of-loop poll + kick_all; standalone upstreaming redundant |
| 0051 | limina: per-vCPU save/restore + snapshot collection (macOS/HVF) for M9 | `src/hvf/src/lib.rs`, `src/vmm/src/lib.rs`, `src/vmm/src/macos/vstate.rs` |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | overlaps #762 | no | with 0052 | host | **redundant/convergent; ours broader** | #762 curates ~38 regs, ours 113 (spike-proven); IDENTICAL exclusions (PAuth in, CVAL not TVAL, EL2/MPIDR out) — no correctness gap found in #762's set. Value = review #762, not replace |
| 0052 | limina: GIC state + RAM dump + snapshot file format (macOS/HVF) for M9 | `src/hvf/src/lib.rs`, `src/vmm/src/lib.rs`, `src/vmm/src/resources.rs` +1 |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | overlaps #762 | no | with 0051 | host | **redundant/convergent** | #762 uses hv_gic_state_* + a 9-register ICC set BYTE-FOR-BYTE the same as ours (SRE/CTLR/PMR/BPR0-1/IGRPEN0-1/AP0R0/AP1R0; RPR+EL2-SRE excluded) — fully convergent on interrupt-delivery-critical capture; only the container differs |
| 0053 | limina: --restore reloads a snapshot into a fresh microVM (macOS/HVF) for M9 | `src/hvf/src/lib.rs`, `src/libkrun/src/lib.rs`, `src/vmm/src/builder.rs` +2 |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | complementary to #762 | no | standalone | host | **complementary — OURS BETTER (multi-vCPU)** | both restore into a fresh VM with the same ordering (MPIDR→GIC→per-vCPU→ICC) + vtimer_mask; **#762 is single-vCPU-only and REJECTS multi-vCPU restore** — our RestoreGate barrier + shared vtimer offset already implement its deferred TODO. Strongest "we're ahead" finding |
| 0054 | limina: PL031 RTC alarm — deliver the match interrupt (macOS/HVF) | `src/devices/src/fdt/aarch64.rs`, `src/devices/src/legacy/rtc_pl031.rs`, `src/vmm/src/builder.rs` +1 |  | yes — bug present in libkrun AND unfixed in Firecracker (vm-superio rtc_pl031.rs stores mr, "not currently implemented", alarm TODO) | libkrun/libkrun@main 2026-08-03 (raw source) + vm-superio@main | none | none | no | standalone | host (enhanced) | keep-downstream / large — portable timer thread + Subscriber + FDT edge-trigger + HVF wiring; macOS/HVF-coupled | Firecracker-inherited dead alarm; upstreamable only with appetite for a full alarm impl |
| 0055 | limina: virtio-net implements reset (suspend/resume thaw), preserving the backend | `src/devices/src/virtio/net/device.rs`, `src/devices/src/virtio/net/worker.rs` |  | yes — VirtioDevice::reset() defaults false, net doesn't override; mmio marks device FAILED (0x8f) on false reset (mmio.rs:273) | upstream main-tip 2026-08-03 | #748 | none — real upstream gap, latent | no | with 0056/0058 | host | **complementary — best standalone candidate** (#762 uses pause/resume, doesn't add reset()) | only bites the re-activate/s2idle path |
| 0056 | virtio: implement reset() for balloon + vsock (suspend/resume thaw) | `src/devices/src/virtio/balloon/device.rs`, `src/devices/src/virtio/mmio.rs`, `src/devices/src/virtio/vsock/device.rs` |  | yes — balloon/vsock don't override reset() on main | upstream main-tip 2026-08-03 | #748 | none | no | with 0055/0058 | host | **complementary** | #762 covers balloon via pause() but NOT vsock at all → vsock-across-snapshot unsupported in both #762 and stock main |
| 0057 | virtio: debug logging for suspend/resume device re-init | `src/devices/src/virtio/mmio.rs`, `src/devices/src/virtio/net/device.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Log taste; s2idle device_status ladder tracing |
| 0058 | virtio: suspend/resume hardening — stop stale threads, drain inactive eventfds | `src/devices/src/virtio/balloon/event_handler.rs`, `src/devices/src/virtio/i2c/event_handler.rs`, `src/devices/src/virtio/mmio.rs` +9 |  | yes — vsock muxer spawns timesync+reaper threads (.run()) with NO deactivate/reset path (present on main); mmio clears only acked_features_select not acked_features (mmio.rs:289) | upstream main-tip 2026-08-03 | #748 | none — **upstreamable hardening, strongest standalone in the cluster** | **yes:memory-corruption** — on re-activation the unstoppable muxer threads write used-ring entries into a freed/realloc'd guest RX ring | with 0055/0056 | host | **complementary — file it** (both sub-fixes independent of the snapshot model) | #762 dodges it (never re-activates vsock) but the latent bug remains; the ack-features clear is a real virtio-1.0 §2.1.1 spec gap on main |
| 0059 | virtio-balloon: gate VIRTIO_BALLOON_F_REPORTING (mask by default) + clear reset state | `src/devices/src/virtio/balloon/device.rs`, `src/vmm/src/builder.rs`, `src/vmm/src/resources.rs` |  | CONDITIONAL — tip still advertises F_REPORTING unconditionally | main tip 2026-08-03 | none (guest-kernel bug, fixed upstream as 0b45f69 per the linux audit) | none — not upstreamable as-is (host workaround for a since-fixed GUEST bug) | no (prevents guest-kernel UAF/oops on s2idle; availability, not host exploit) | standalone | host safety valve | **CARRY as host safety valve; relax the default once min guest kernel ≥ 0b45f69** | NO libkrun issue on balloon+suspend exists; F_REPORTING_PM_SAFE Bit 6 is still an RFC, doesn't change disposition today |
| 0060 | gpio: add a KEY_SLEEP suspend button (host-driven, no guest agent) | `src/devices/src/fdt/aarch64.rs`, `src/devices/src/legacy/aarch64/gpio.rs`, `src/libkrun/src/lib.rs` +2 |  | LIMINA-NEW button on the EXISTING upstream PL061 gpio (legacy/aarch64/gpio.rs exists upstream, single-button) | main @c652b56 2026-08-03 [LIMINA-NEW] | none | none | no — host-driven suspend eventfd, no new guest→host data path (PL061 MMIO regs unchanged) | **fold-into:0061** (0060 line-4/KEY_SLEEP layout is entirely RE-LAID by 0061 → throwaway intermediate) | host (guest = stock gpio-keys + systemd-logind) | CARRY — KEY_SLEEP suspend button + suspend_efd for M9 stock-tier freeze | clean naming |
| 0061 | gpio: split into poweroff (KEY_POWER), suspend (KEY_SLEEP), restart (KEY_RESTART) | `src/devices/src/fdt/aarch64.rs`, `src/devices/src/legacy/aarch64/gpio.rs`, `src/libkrun/src/lib.rs` +2 |  | LIMINA-NEW (3-button split) BUT carries a SHARED upstream bug: upstream sole button emits KEY_RESTART (logind reboot) off shutdown_efd though the module intent is "gracefully poweroff" → stock-logind guest reboots instead of powering off | main @c652b56 2026-08-03 [SHARED-BUG(KEY_POWER relabel) + LIMINA-NEW(split)] | none | none | no | **head commit — absorbs 0060** into "gpio: host-driven lifecycle buttons"; BUT carve the KEY_POWER relabel into a standalone upstreamable patch | host | **SPLIT: KEY_POWER-vs-KEY_RESTART carve-out = UPSTREAM-AFTER-CLEANUP** (the one send-now in C8); carry the suspend/restart split | caveat: latent upstream because typical libkrun guests run no logind — verify upstream cares before proposing |
| 0062 | gpio: add a wakeup-source KEY_WAKEUP button (M9 restore wake) | `src/devices/src/fdt/aarch64.rs`, `src/devices/src/legacy/aarch64/gpio.rs`, `src/libkrun/src/lib.rs` +2 |  | LIMINA-NEW wakeup-source button on the existing PL061 | main @c652b56 2026-08-03 [LIMINA-NEW] | none | none | no — host-driven wake eventfd | **fold-into:0061** (folds cleanly with 0060/0061) | host (guest = stock gpio-keys, arms enable_irq_wake) | CARRY — KEY_WAKEUP + wakeup-source FDT prop, the only line that exits s2idle (M9 restore wake) | follow-on dep: PL061/PL031 register-state serialize for snapshot demux done later (0063) |
| 0063 | snapshot: serialize the PL061 GPIO register file (M9.2 restore wake) | `src/devices/src/legacy/aarch64/gpio.rs`, `src/devices/src/legacy/mod.rs`, `src/vmm/src/builder.rs` +3 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Serializes PL061 state for buttons only we add (0060–0062) |
| 0064 | vmm: add resume_parked_vcpus for the M9 snapshot abort path (macOS) | `src/vmm/src/lib.rs` |  | fold — abort-path fixup: resume_parked_vcpus for the M9 snapshot abort | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0050** (vCPU pause/quiesce) | host | fold | completes 0050's quiesce; 0050 is redundant vs #762 → this rides the carry/redundant verdict |
| 0065 | vmm: virtio quiesce oracle for the M9.2 suspend bracket (macOS) | `src/devices/src/virtio/mmio.rs`, `src/vmm/src/device_manager/hvf/mmio.rs`, `src/vmm/src/lib.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Quiesce oracle for limina's suspend bracket policy |
| 0066 | vmm: M9.3 SHM-window-fault oracle on the HVF MMIO path (macOS) | `src/vmm/src/lib.rs`, `src/vmm/src/macos/vstate.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | One-shot SHM-window-fault warn; M9.3 floor-spike oracle |
| 0067 | vmm: M9 snapshot byte-section length must be u64, not u32 (macOS) | `src/vmm/src/snapshot.rs` |  | fold — format bugfix: snapshot byte-section length must be u64 not u32 | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0052** (GIC/RAM snapshot format) | host | fold | a size-field correction to 0052's own format; belongs in the original commit |
| 0068 | vmm/test: add balloon_free_page_reporting to default_vm_resources fixture | `src/vmm/src/resources.rs` |  | fold — test-fixture fixup: add balloon_free_page_reporting to default_vm_resources | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0059** (gate F_REPORTING) | host | fold | test plumbing for 0059; rides it |
| 0069 | vmm: M9.3 snapshot v4 format — layout-identity header + device-transport section | `src/vmm/src/lib.rs`, `src/vmm/src/snapshot.rs` |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | complementary to #762 | no | with 0070 | host | **complementary — merges cleanly** | both fail CLOSED on layout drift; ours finer on addresses (exact ram_start/last/shm_start + firmware), #762 finer on device-topology ordering (added after mtjhrc review) |
| 0070 | vmm: M9.3 capture device transports + restore validation (macOS) | `src/devices/src/virtio/mmio.rs`, `src/vmm/src/builder.rs`, `src/vmm/src/device_manager/hvf/mmio.rs` +2 |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | complementary — different model | no | with 0055/0056 | host | **complementary — design divergence** | #762 host-restores device state (pause→queues→resume/save_state); WE deliberately don't replay — the guest re-negotiates every device on s2idle thaw. Note: #762's pause() stops workers before capture, solving our torn-dump_ram residual — cleaner there |
| 0071 | limina(0071): M9.3 GPU probes — stale-ctx/resource counters, fence ledger, renderer dump | `src/devices/src/virtio/gpu/mod.rs`, `src/devices/src/virtio/gpu/trace.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` +3 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | strip-diag-first | host | carry | LIMINA_GPU_TRACE counters + limina_dump_state FFI into our virgl fork (env-gated, no /tmp) |
| 0072 | limina(0072): sticky queue re-arm — survive the no-PM-ops bus-fallback s2idle resume | `src/devices/src/virtio/mmio.rs`, `src/vmm/src/device_manager/hvf/mmio.rs` |  | yes | #762 diff (PR file-touch) 2026-08-03 (gpu deferred in #762) | #748 | complementary — #762 defers gpu entirely | no | standalone | host | **complementary — #762 doesn't cover gpu** | root cause is upstream-Linux (virtio-gpu has no PM freeze/restore ops through v7.1.2) — a Linux-side report; no sign anyone upstream hit the bus-fallback DRIVER_OK-on-dead-queues wedge |
| 0073 | limina(0073): rutabaga-layer snapshot-replay journal (M9.3 P0) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/mod.rs`, `src/devices/src/virtio/gpu/trace.rs` +2 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | limina venus snapshot-replay journal; pairs fork vkr_journal.c |
| 0074 | limina(0074): v5 GPU snapshot section + venus replay engine (M9.3 P1) | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` +6 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | v5 GPU section + replay engine; fork VKJR export |
| 0075 | limina(0075): drain the guest fence ledger before snapshot capture (M9.3 P2) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` +1 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Fence-ledger drain before capture; chained 0071/0074 |
| 0076 | limina(0076): classify replay wire-entry drops by journal class (M9.3 diagnostics) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Replay drop classification diagnostics on 0074 |
| 0077 | limina(0077): block the thaw's DRIVER_OK until the staged snapshot replay finishes (M9.3) | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | DRIVER_OK blocked on staged replay; limina restore ordering |
| 0078 | limina(0078): release the vkr blob-alloc journal pin at global resource unref (M9.3) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` +2 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Journal-pin lifetime via limina_journal_unpin FFI |
| 0079 | limina(0079): full VkDeviceMemory content capture in the GPU snapshot (M9.3 P2) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/rutabaga_gfx/src/rutabaga_core.rs` +1 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | VkDeviceMemory content capture via rutabaga limina_memory_census |
| 0080 | limina(0080): sync-state section in the GPU snapshot (M9.3 P2.1) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/rutabaga_gfx/src/rutabaga_core.rs` +1 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | vkr sync-state section; fork sync fast-forward |
| 0081 | vmm: v6 chunked-frame snapshot format — parallel lz4 + zero-page holes (limina M9.4 snapshot speed) | `Cargo.lock`, `src/vmm/Cargo.toml`, `src/vmm/src/builder.rs` +2 |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | **OURS BETTER — sendable to #762** | no | with 0084 | host | **contribute the delta** | #762 writes raw whole-RAM memory.img — no compression, no zero-hole, no per-region CRC; our v6 (holes+parallel lz4+CRC32, 54s→6.6s, 8.8GB→465MiB) is a clean self-contained upgrade |
| 0082 | vmm: lz4 the GPU re-creation section in the snapshot head (limina M9.3 P3) | `src/vmm/src/snapshot.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | lz4 of the limina-only LGPU payload |
| 0083 | gpu: carry the cursor overlay across snapshot restore (limina M9.3 P3) | `src/devices/src/virtio/gpu/journal.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Cursor overlay across restore; chained to 0008 + replay payload |
| 0084 | vmm: publish the snapshot atomically (stream to .tmp, rename on success) | `src/vmm/src/lib.rs` |  | yes | #762 diff (PR file-touch) 2026-08-03 | #748 | **OURS BETTER — trivially sendable** | no | with 0081 | host | **contribute** | #762 writes manifest/vmstate/memory.img directly into the dir, no tmp+rename → torn snapshot on a mid-write crash; our atomic publish adapts to a dir (write dir.tmp, rename), low-risk |
| 0085 | vmm: fast lz4 (default-features off) + snapshot phase timing | `Cargo.lock`, `src/vmm/Cargo.toml`, `src/vmm/src/builder.rs` +1 |  | fold — perf fixup: fast lz4 (default-features off) + snapshot phase timing | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0081** (v6 chunked snapshot) | host | fold | a dependency/timing refinement of 0081's v6 format; 0081 is the "contribute the delta to #762" row → fold in first |
| 0086 | gpu: rebase CreateBlob vkr_seq fences into the new journal epoch at replay | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Cross-epoch vkr_seq rebase; chained fixup on 0073/0074 |
| 0087 | balloon: stop advertising DEFLATE_ON_OOM by default (per-VM re-advertise knob) | `src/devices/src/virtio/balloon/device.rs`, `src/vmm/src/builder.rs`, `src/vmm/src/resources.rs` |  | yes — tip AVAIL_FEATURES does NOT advertise DEFLATE_ON_OOM (our 0034 added it, 0087 removed it + made it a per-VM knob → nets back to upstream default + a toggle) | main tip 2026-08-03 | **#707 OPEN "Support virtio-balloon deflate-on-oom flag"** — asks for exactly this toggle (muvm desktop) | none | no | standalone | host | **CARRY + strong upstream candidate (resolves #707)** | the only debate is default: we argue OFF (transparent MemTotal accounting + systemd-oomd); #707's muvm wants it ON — same knob satisfies both |
| 0088 | rtc_pl031: anchor reads to the host wallclock, not a monotonic Instant | `src/devices/src/legacy/rtc_pl031.rs` |  | yes — bug present (rtc_pl031.rs:59,90 still Instant/duration_since — freezes across host sleep) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | no | **layered on 0054's struct rewrite** — diff won't apply without 0054 | host | send after 0054 (or re-express against the stock two-field struct to send standalone) |  |
| 0089 | gpu: defer-and-classify the session reset — park across a bus-fallback thaw | `src/devices/src/virtio/device.rs`, `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` +2 |  | limina-shaped — depends on limina reset()+MMIO rearm-stash (set_thaw_activation), none upstream | main @c652b56 2026-08-03 | pairs 0072 (bus-fallback thaw) | none | no | standalone | host (venus/s2idle) | CARRY — too venus/suspend-shaped; the parked-session concept doesn't generalize without limina's s2idle stack | not an upstream candidate |
| 0090 | fs: report bytes-written as the virtio used-ring length | `src/devices/src/virtio/fs/worker.rs` |  | **superseded-upstream@4e01b23** — fs/worker.rs now threads len into add_used(.., len) gated on needs_notification | libkrun/libkrun@main 2026-08-03 (raw source) | Resolves containers/libkrun#695 (cites kernel 68b69fa0edb) | 4e01b23 (Gris Ge) | no | drop on rebase | host | **DROP — already upstream** | bug + blast radius were exactly as our note claimed; just redundant now |
| 0091 | gpu: negotiate EVENT_IDX and coalesce used-buffer interrupts | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | yes — bug fully present (GPU avail_features lacks EVENT_IDX; fence handler signals per-retired-descriptor in the loop, vgpu.rs:203) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | no | standalone | host | rework-then-send (B) — high value | upstream negotiates EVENT_IDX in fs+block+net (idiom d264000); GPU is the conspicuous omission — idiomatic not novel; needs re-derivation vs the refactored process_queue worker |
| 0092 | polly/gpu: env-gated wake-rate tracing (LIMINA_WAKE_TRACE) | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs`, `src/polly/src/event_manager.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | strip-diag-first | host | carry | LIMINA_WAKE_TRACE per-fd wake attribution (env-gated, no /tmp) |
| 0093 | vsock timesync: saturate elapsed-time arithmetic against backward clock steps | `src/devices/src/virtio/vsock/timesync.rs` |  | yes — bug present (timesync.rs:72 identical raw subtraction → debug-build overflow panic on backward CLOCK_REALTIME) | libkrun/libkrun@main 2026-08-03 (raw source) | none | none | no | standalone | host | **send-now — cleanest in the cluster** (extract sync_due() with saturating_sub + 3 unit tests) |  |
| 0094 | hvf/vmm: model PSCI CPU_OFF + AFFINITY_INFO and re-deliverable CPU_ON (guest vCPU hotplug) | `src/devices/src/legacy/vcpu.rs`, `src/hvf/src/lib.rs`, `src/vmm/src/builder.rs` +1 |  | yes — bug present and WORSE on raw tip: no CPU_OFF/AFFINITY_INFO arm, so guest `echo 0 > cpuN/online` hits the 0028 `val => panic!` and PANICS the VMM (the 546% wedge is post-0028 behavior) | upstream main HEAD 2026-08-03 (raw file reads) | none | needs rework (sizeable: CpuOff exit, park/reonline, boot-sender fan-out) | med-high (guest-triggerable VMM abort/wedge DoS) | standalone | host | upstream-worthy but large — **sequence AFTER 0028** | 0028 first turns the panic into NOT_SUPPORTED, then 0094 adds real hotplug; snapshot-while-offline limitation documented in-patch |
| 0095 | usb/xhci: emulated xHCI controller bring-up (register file, stub data path) | `src/devices/Cargo.toml`, `src/devices/src/fdt/aarch64.rs`, `src/devices/src/lib.rs` +8 |  | new-feature | libkrun/libkrun 2026-08-03 (search + source) (0 xhci/usb issues) | none | none | no | tranche 1 (controller+framework, atomic) | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | generic-xhci MMIO, feature-gated usb, off by default; Linux issues 0 commands until a device connects so the register-file alone brings the HCD up; gkvm/limina naming in device_manager |
| 0096 | usb/xhci: Stage B1 — rings, command set, EP0 control transfers, gadgets | `src/devices/src/usb/mock.rs`, `src/devices/src/usb/mod.rs`, `src/devices/src/usb/model.rs` +10 |  | new-feature | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 1; **ships with 0101** | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | defines UsbDeviceModel seam (policy-free defaults) — NO upstream trait to match (xHCI isn't virtio; VirtioDevice is the only cousin); our invention, defensible as new; deferred-call invariant (gadget called only after lock release) |
| 0097 | usb/xhci: Stage B2 — non-EP0 data flow + mock HID echo gadget | `src/devices/src/usb/hid.rs`, `src/devices/src/usb/mod.rs`, `src/devices/src/usb/model.rs` +3 |  | new-feature | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 1 | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | non-EP0 data flow + per-endpoint generation counters for in-flight staleness; defensible vs QEMU (QEMU tracks live XHCITransfer per-EP; our monotonic epoch is the same guarantee, cleaner for a lock-released worker); mock HID 0x1d6b:0x0f11 |
| 0098 | usb/xhci: generic HID report-pipe gadget (Stage C mechanism) | `src/devices/src/usb/mod.rs`, `src/devices/src/usb/report_pipe.rs` |  | new-feature | libkrun/libkrun 2026-08-03 (search + source) (hid→0) | none | none | no | tranche 2 (gadget mechanisms) | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | generic HID report-pipe, NO identity (identity = limina policy); likely reviewer objection "prefer real passthrough" — rebuttal: host has no USB/IP, macOS device-claim needs root/entitlements |
| 0099 | usb/xhci: generic bulk-pipe gadget (MOC fingerprint mechanism) | `src/devices/src/usb/bulk_pipe.rs`, `src/devices/src/usb/mod.rs` |  | new-feature | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 2 | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | generic variable-length multi-IN bulk gadget (MOC MECHANISM; elanmoc identity NOT here — limina policy); same passthrough objection |
| 0100 | usb/xhci: handle Immediate Data (IDT) on Normal TRBs | `src/devices/src/usb/xhci/engine.rs` |  | spec-conformance (xHCI §4.11.7) | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 1 | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | IDT on Normal TRBs — reading matches spec + QEMU (xhci_xfer_data IDT check); needed by any real small-bulk-OUT device; has regression test |
| 0101 | usb/xhci: bound work TRBs per data TD (hostile-ring DoS guard) | `src/devices/src/usb/xhci/engine.rs`, `src/devices/src/usb/xhci/trb.rs` |  | **SECURITY** — hostile-ring guard | libkrun/libkrun 2026-08-03 (search + source) | none | none | yes:DoS (unbounded Chain bit grows buffers, hangs the worker) | **MUST-fold-with 0096/0097 — never submit them without it** | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | cap TD at MAX_TD_TRBS=1024, RingError::TdTooLong; exactly the guard class upstream merges (cf. #577/#329/#790 hostile-guest hardening) |
| 0102 | usb/xhci: PM-correct register semantics (suspend/resume) | `src/devices/src/usb/xhci/device.rs`, `src/devices/src/usb/xhci/engine.rs`, `src/devices/src/usb/xhci/trb.rs` |  | robustness/PM | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 3 | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | seven register-file bugs vs Linux xhci_suspend/resume incl. a PERMANENT BRICK from a command-ring CNR timeout; aligns with QEMU PM register semantics (CSS-CRS, self-clearing CRCR); reviewer-credible as correctness |
| 0103 | usb/xhci: carry controller state across a VM snapshot | `src/devices/src/lib.rs`, `src/devices/src/usb/xhci/device.rs`, `src/devices/src/usb/xhci/engine.rs` +7 |  | snapshot-state | libkrun/libkrun 2026-08-03 (search + source) | **depends-on #762** | none | no | tranche 3 | host | OFFER (BLOCKED on #762) — #762 is virtio-transport-centric and defers most per-device state; 0103 both depends on #762 landing AND extends it to a non-virtio platform device | needs the #762 seam finalized + a pause/save/resume-shaped quiesce for xHCI; hold until C1/#762 resolves |
| 0104 | usb/xhci: trace the PM register writes (suspend/resume oracle) | `src/devices/src/usb/xhci/device.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Unconditional debug trace of USBCMD/PORTSC; our only PM oracle — keep, but log taste upstream |
| 0105 | usb/xhci: an abort cancels a command doorbell queued before it | `src/devices/src/usb/xhci/device.rs`, `src/devices/src/usb/xhci/engine.rs` |  | ordering-bug | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 3 (pairs 0102) | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | abort must cancel a command doorbell queued before it; same concern QEMU addresses via xhci_abort_command/CRCR stop; strengthened round-trip test (46 RED cases, scripts/xhci-red-check.py) |
| 0106 | virtio: LIMINA_WAKE_PROBE — time the VMM half of the venus wake chain | `src/devices/src/virtio/gpu/worker.rs`, `src/devices/src/virtio/mmio.rs`, `src/devices/src/virtio/mod.rs` +1 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | strip-diag-first | host | carry | LIMINA_WAKE_PROBE doorbell→wake timing (env-gated, no /tmp) |
| 0107 | virtio: close the wake chain's return half via the virtio ISR ack | `src/devices/src/legacy/vcpu.rs`, `src/devices/src/virtio/gpu/worker.rs`, `src/devices/src/virtio/mmio.rs` +2 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Return half of 0106 via virtio ISR ack (reg 0x64) |
| 0108 | virtio: attribute control-queue drains that overrun a frame | `src/devices/src/virtio/gpu/worker.rs`, `src/devices/src/virtio/wake_probe.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Outlier attribution for control-queue drains; extends 0106 |
| 0109 | virtio: stamp every wake-probe line with CLOCK_REALTIME, not just the outliers | `src/devices/src/virtio/wake_probe.rs` |  | fold — probe fixup: stamp every wake-probe line with CLOCK_REALTIME, not just outliers | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0106** (LIMINA_WAKE_PROBE) | host | fold | DIAG-class refinement of 0106; strip-diag with the parent |
| 0110 | virtio-gpu: default fence-accurate presents ON when the shown-ack channel exists | `src/devices/src/virtio/gpu/virtio_gpu.rs` | DIAG | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | strip-diag-first | host | carry | Policy keyed on LIMINA_SHOWN_ACK_FD + /tmp marker |
| 0111 | virtio-gpu: readback fallback for deferred presents on sinks without zero-copy | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Readback fallback for the headless capture sink; chained 0017/0110 |
| 0112 | virtio-gpu: swap the fence-present runtime marker to a force-OFF | `src/devices/src/virtio/gpu/virtio_gpu.rs` | DIAG | fold — policy fixup: swap the fence-present runtime marker to a force-OFF | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0110** (default fence-present on shown-ack) | host | fold | DIAG-class marker change on 0110; strip-diag with the parent |
| 0113 | virtio-gpu: poll the fence-present force-off marker off the flush path | `src/devices/src/virtio/gpu/virtio_gpu.rs` | DIAG | fold — follow-up to 0112: poll the force-off marker off the flush path | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0110/0112** | host | fold | DIAG-class; folds together with 0112 into 0110 |
| 0114 | virtio-gpu: log fence-present engagement once at INFO | `src/devices/src/virtio/gpu/virtio_gpu.rs` |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | Log taste; one INFO line on first deferred present |
| 0115 | virtio-gpu: resolve + present vrend IOSurface scanouts on plain SET_SCANOUT | `src/cpuid/src/common.rs`, `src/devices/src/legacy/rtc_pl031.rs`, `src/devices/src/usb/hid.rs` +13 |  | needed | n/a (local triage 2026-08-03) | n/a | n/a | no | standalone | host | carry | vrend zero-copy scanout; needs fork sync_iosurface/IOSurface exports |
| 0116 | virtio-gpu: honest Global-ring fences for vrend sessions | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | fold — correctness follow-up: honest Global-ring fences for vrend sessions | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0010** (coexist fence routing) | host | fold | extends 0010's fence routing to vrend; rides 0010 (CARRY, macOS/coexist) |
| 0117 | virtio-gpu: retire-as-lost for refused context fences (the §22 KMS wedge) | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | SEC invariant — upstream has no context-fence-failure recovery; a refused fence makes the guest dma_fence vanish → KMS wedge; invariant NOT honored upstream (but the park/replay path is limina) | main @c652b56 2026-08-03 | none | none | yes:never-signaling guest fence wedges DRM for every later session (DoS) | standalone | host | CARRY (retire-as-lost on limina fence machinery); **the "a guest fence must never vanish" invariant is a valid upstream PRINCIPLE** even if the code isn't portable | caught live on dogfood-mac 2026-07-30; test seams LIMINA_GPU_TEST_* inert/env-gated |
| 0118 | virtio-gpu: drain-then-classify device resets; name the non-quiescent half | `src/devices/src/virtio/gpu/virtio_gpu.rs`, `src/devices/src/virtio/gpu/worker.rs` |  | fold — follow-up to 0089: drain-then-classify device resets, name the non-quiescent half | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0089** (park across bus-fallback thaw) | host | fold | belongs in 0089's reset-classify body; CARRY (limina s2idle) |
| 0119 | virtio-gpu: stable EDID identity, real mode list and monitor range limits | `src/devices/src/virtio/gpu/display.rs`, `src/devices/src/virtio/gpu/edid.rs` |  | yes — generator at tip carries BOTH bugs: (a) populate_standard_timings writes aspect_ratio_bits UNSHIFTED (needs <<6) → 1600x900 advertised 16:10@63Hz; (b) clock u16 cast TRUNCATES/wraps the 16-bit pixel clock (no saturate). Identity/range/mode-list parts are limina-new | main @c652b56 2026-08-03 [SHARED-BUG x2] | overlaps OPEN PR #786 (display name/mfr) | none | no | **absorbs 0121** (descriptor-overflow log) | host | **SPLIT: the two generator bugs = UPSTREAM-NOW** (clean standalone generator-fix PR); identity/range/mode-list bulk = CARRY | #319 moved edid.rs→src/devices/src/display/edid.rs but body byte-identical; upstream EdidParams has only refresh_rate+physical_size (no range field); Provided(Box<[u8]>) variant is an alt identity path |
| 0120 | virtio-gpu: runtime display updates carry EDID and connection state | `src/devices/src/virtio/gpu/device.rs`, `src/devices/src/virtio/gpu/display.rs`, `src/devices/src/virtio/gpu/virtio_gpu.rs` +1 |  | yes — display_info() maps |d| (d.width,d.height,true): connected/enabled flag HARDCODED true → real unplug can't be signalled | main @c652b56 2026-08-03 [SHARED-BUG latent] | none | none | no | rides 0025 | host | CARRY-with-0025 — the fix needs 0025's DisplayUpdate{size,edid,connected} queue; not standalone-upstreamable | hardcoded-true confirmed at virtio_gpu.rs:550-557 |
| 0121 | virtio-gpu: log when descriptor slots overflow the base EDID block | `src/devices/src/virtio/gpu/edid.rs`, `src/devices/src/virtio/gpu/mod.rs` |  | fold — log fixup: warn when descriptor slots overflow the base EDID block | n/a (fold pass 2026-08-03) | n/a | n/a | no | **fold-into:0119** (stable EDID identity) | host | fold | README already lists 0119 as absorbing 0121; a diagnostic for 0119's mode-list packing |
| 0122 | virtio-gpu: declare the generated EDID digital, not analog | `src/devices/src/virtio/gpu/edid.rs` |  | yes — generator never writes edid[20] → byte20==0==valid ANALOG input; every parser reports analog, no color depth | main @c652b56 2026-08-03 [SHARED-BUG] | none | none | no | standalone | host | **UPSTREAM-NOW** — trivial generator fix (set digital/8bpc/DP); the one deliberately non-byte-identical default | clean send-now |
| 0123 | virtio-gpu: carry over-ceiling modes in a DisplayID type VII extension block | `src/devices/src/virtio/gpu/edid.rs` |  | yes — upstream emits a single 128-byte EDID, no extension blocks, no over-ceiling advertisement → purely limina-new feature | main @c652b56 2026-08-03 [LIMINA-NEW feature] | none | none | no | standalone | host | CARRY (feature proposal at best) — no DisplayID precedent in libkrun (generator descends from ChromiumOS/crosvm, same single-block lineage as QEMU) | CTA-861 can't lift the 655.35MHz ceiling; DisplayID type VII (24-bit kHz clock) is the vehicle |
| 0124 | virtio-gpu: carry a range descriptor's horizontal rate past 255 kHz | `src/devices/src/virtio/gpu/display.rs`, `src/devices/src/virtio/gpu/edid.rs` |  | yes — but LIMINA-INTERNAL: upstream generator emits NO range-limits descriptor (blocks 2&3 zero); EdidParams has no range/RangeSpec type → nothing upstream to clamp | main @c652b56 2026-08-03 [LIMINA-INTERNAL] | none | none | no | rides 0119 | host | CARRY — corrects the going-in hypothesis: NOT a shared-generator send-now; it widens limina's own 0119-added RangeSpec (min/max_horizontal_khz u8→u16 + EDID-1.4 +255 offset flags) | no upstream range descriptor exists to be "clamped at 255" |
| 0125 | virtio-console: return a port's queues on close so it can be reopened | `src/devices/src/virtio/console/device.rs`, `src/devices/src/virtio/console/port.rs`, `src/devices/src/virtio/console/process_rx.rs` +1 |  | yes — bug present (tip device.rs .expect("port rx queue should exist") :266/:270; shutdown()->() :358) | upstream main HEAD 2026-08-03 (raw file reads) | none | clean, self-contained | med (guest-triggerable VMM abort; systemctl restart spice-vdagentd / dd /dev/vportNpM — same class as 0028/#577) | standalone | host | **cleanest upstream bug report + PR of the cluster** — device-generic, no limina coupling, ships with l1_port_reopen.rs regression test |  |
| 0126 | usb: tell a gadget when the guest cancels an endpoint's transfer | `src/devices/src/usb/bulk_pipe.rs`, `src/devices/src/usb/mod.rs`, `src/devices/src/usb/model.rs` +2 |  | new-mechanism | libkrun/libkrun 2026-08-03 (search + source) | none | none | no | tranche 3 (extends the 0096 trait) | host | **OFFER as RFC first** — upstream has zero USB prior art/demand | endpoint_stopped(EpAddr), default no-op; well-precedented — QEMU's USBDeviceClass has ->cancel_packet(), direct analogue |

## Findings

### Post-audit commits (unaudited — added after the 2026-08-03 pass / the fork migration)

The table's 126 rows are the audited series (124 commits at migration). The branch now carries
127 commits above upstream `07fd40dc`; the three below have not been researched against upstream.
Listed so the drift is visible rather than implied by a stale count:

| commit | subject | first guess at disposition |
|---|---|---|
| `aeafaf2` | vmm/macos: service pause/snapshot from the secondary boot wait | **send-later** — folded into the PSCI CPU_ON commit (`5a44abc`) at the 2026-08-12 fixup-squash; the deadlock is an interaction with our carried PSCI CPU_ON boot-channel park (0094-era), so it travels with that series; see the task #21 note above |
| `bfc332a` | virtio-gpu: ledger the display path's scanout resources | carry (DIAG-class, `[SCANOUT-LEDGER]` at debug) — but the `stranded` count it added names a real upstream-shared bug: `unref_resource` drops a resource's metadata *before* refusing a still-scanned-out unref. Worth a standalone upstream fix if that path is ever confirmed reachable |
| `d9afca2` | virtio/gpu: tell the display backend when a scanout resource is unref'd | carry (chains onto 0001's display-backend vtable, which is limina-shaped) — the *mechanism* (a release edge on the display callback, so a host-side backend can drop its reference) is generic and would interest any out-of-process display backend; unsendable until the vtable itself is |

### Series verdict (all 126 rows researched 2026-08-03, vs main `c652b56`) — COMPLETE

All 8 research clusters (C1–C8) banked + the 36 carry rows + the 15 fold rows. Disposition
roll-up (by `disp`, fold rows counted under their parent):

**DROP — already fixed upstream, retire on rebase (3):**
- **0038** (virtio-blk serial) → upstream `df85b8b` (Ryan Dunn) folded caller block_id into
  build_disk_image_id, semantically identical.
- **0044** (blob-map overflow) → upstream `checked_blob_map_addr` (PR #790, independent
  third party). **Disclosure gate CLEARED** — handled publicly, not exploitable on tip, no embargo.
- **0090** (virtio-fs used-ring length) → upstream `4e01b23` (Gris Ge), resolves libkrun#695.

**UPSTREAM-NOW / send-now — clean guest-visible bug on shared code, no limina coupling (front of the
engagement queue):**
- **0028** (unhandled-PSCI `panic!`) — FLAGSHIP/umbrella DoS hardening; reachable on STOCK upstream;
  cite open #577. Neutralizes the whole panic surface (incl. 0004, 0094's).
- **0125** (console port-reopen `expect()` abort) — same class, device-generic, ships with a test.
- **0090**'s twin bucket aside, the other trivial correctness sends: **0004** (halfword-MMIO write arm,
  pairs 0005), **0040** (vtimer multiply-before-divide), **0093** (timesync saturating_sub),
  **0037** (input reset→Inactive), **0039** (input epoll block), **0014**+**0024** (GPU readback/
  transfer_read depanic), **0041** (macOS blob unmap balance), **0031** (display -2 enum arm),
  **0122** (EDID digital not analog), **0119**-generator-subset (unshifted aspect + u16 clock wrap),
  **0027** (de-shear, strip DIAG first), **0029** (capset count — attaches to OPEN #565 + PR #560),
  **0008**+**0015** (cursor-queue depanic), **0061**-carve-out (KEY_POWER not KEY_RESTART).

**UPSTREAM-AFTER-CLEANUP — mechanism upstreamable, strip DIAG or rework first:** 0025 (resize, 3 /tmp
hooks), 0091 (EVENT_IDX, re-derive vs refactored worker), 0023 (reboot exit code), 0006 (mmio
QueueNum leniency — discuss), 0002 (PL011 WouldBlock — discuss), 0054 (PL031 alarm — large).

**CONVERGENT vs in-flight upstream PRs — review/contribute the delta, don't re-submit:**
- **#762** (HVF snapshot/restore) covers **0050/0051/0052/0069/0070**; ours is broader
  (**0053** multi-vCPU restore that #762 explicitly rejects; **0081/0084** compression + atomic
  publish it lacks — contribute those deltas). **0055/0056/0058** add `reset()` #762 doesn't
  (0058 is a real memory-corruption hardening, strongest standalone).
- **#794** (balloon reclaim) converges with **0033**'s madvise switch (maintainer thread favors us);
  **0034/0087** are orthogonal control-plane (0087 resolves OPEN #707).

**CARRY — limina/macOS-shaped, no upstream home (the bulk):** the whole GPU coexist/venus/IOSurface
stack (0001/0010/0012/0013/0032/0115…), the M9.3 GPU snapshot-replay journal (0071–0086), the reset
lifecycle (0007/0022/0035/0089/0117/0118), all log-taste rows, and every DIAG probe.

**NEW-FEATURE RFC — no upstream prior art or demand, offer as RFC:** the entire USB/xHCI tranche
(0095–0105, 0126) and all of C8's new devices (0042 virtio-i2c/battery, 0047–0049 virtio-snd,
0060–0062 gpio buttons). Guest side is stock Linux for every one (tier=host); PR #749 shows upstream
moving *away* from sound, so virtio-snd is a genuinely new shape, not a supersession.

**Security (16 sec:yes rows)** — see the dedicated section below; 0028/0125/0058/0094 are the
guest-triggerable-abort front line, all with public-issue cover (#577), no embargo needed.

### Triage summary (2026-08-03, local reading only — superseded by the verdict above)

126 patches classified into three buckets. Counts: **research-needed 75**, **carry-obvious 36**,
**fold-candidate 15**. (All now filled; kept for the cluster map below.) Rows keyed by SUBJECT.

### Research clusters (for the browser units)

- **C1 — Snapshot / suspend-resume vs upstream PR #762 (HVF snapshot/restore) + issue #748 RFC.
  HIGHEST PRIORITY** (11): 0050, 0051, 0052, 0053, 0069, 0070, 0081, 0084, 0055, 0056, 0058, 0072.
  (0072 also asks whether virtio-gpu's missing Linux PM ops / bus-fallback DRIVER_OK-on-dead-queues
  wedge is a known upstream issue.)
- **C2 — Balloon / dynamic memory vs upstream PR #794 (reclaim via stage-2 unmap)** (4): 0033, 0034,
  0059, 0087. (0059 also = an upstream Linux `virtballoon_freeze` page-reporting UAF report.)
- **C3 — HVF / PSCI / MMIO robustness + the panic→graceful DoS class. SECURITY-LED** (11): 0028
  (flagship — check libkrun HEAD converted the `val => panic!` since base), 0004, 0023, 0040, 0043
  (vs slp's F_BLOB_ALIGNMENT), 0044 (disclosure gate — see below), 0012, 0094, 0125, 0014/0024,
  0041.
- **C4 — virtio device correctness & perf, non-GPU** (12): 0090 (highest-value send-now), 0091,
  0093, 0088, 0054, 0037, 0039, 0038, 0002, 0005, 0006, (0058 cross-listed C1).
- **C5 — virtio-gpu core: 2D scanout, cursor, coexist, reset lifecycle** (12): 0001, 0008, 0015,
  0010, 0012/0013, 0022 (+folds 0007/0035), 0027, 0029, 0035, 0089, 0117.
- **C6 — Display, resize, EDID** (7): 0031, 0025, 0119, 0120, 0122, 0123, 0124.
- **C7 — USB / xHCI (biggest feature offer)** (11): 0095, 0096, 0097, 0098, 0099, 0100, 0101, 0102,
  0103, 0105, 0126.
- **C8 — Other new devices + guest-lifecycle buttons** (7): 0042 (virtio-i2c/SBS battery), 0047,
  0048, 0049 (virtio-snd), 0060, 0061, 0062 (gpio suspend/restart/wakeup). DONE: all LIMINA-NEW
  host-side additions, guest stock; upstream has no i2c/ or snd/ dir, PR #749 removes the SND doc
  mention; one send-now carve-out (0061 KEY_POWER-not-KEY_RESTART). Fold snd 0047→0048 (+0049
  separable), gpio 0060/0062→0061.

### Fold map (15 entries; executed as one re-export at series end)

0007→0022 · 0020→0013/0017 · 0021→0017 · 0036→0017/0027 · 0045→0033 · 0064→0050 · 0067→0052 ·
0068→0059 · 0085→0081 · 0109→0106 · 0112→0110 · 0113→0110/0112 · 0116→0010 · 0118→0089 · 0121→0119.

### DISCLOSURE GATE — libkrun 0044 / 0012 (READ BEFORE ANY resource_map_blob PR)

**The blob-map `offset+size` overflow FIX has landed (`patches/libkrun/0044`, RED/GREEN unit-tested,
2026-07-04) but its EXPLOITABILITY IS UNRESOLVED** per `docs/upstreaming/00-obvious-fixes-and-security.md`
§B3. Open question: does a wrapped guest-controlled `offset` actually reach `hv_vm_map` with a mapping
placed OUTSIDE the SHM window, or does HVF/alignment reject it first? Until a C3 research deliverable
answers that:

- **0044's `mr` column must read `blocked-on-disclosure`** — if it proves exploitable against upstream
  libkrun's `resource_map_blob` it is CVE-class → private report + embargo to libkrun/libkrun, NOT a
  public PR.
- **0012 is gated on the same question**: it broadened the vulnerable path from the old Apple-gated fd
  type to EVERY venus blob, so it must not go public ahead of the disclosure decision either.
- **NO public PR touching `resource_map_blob` until that question is answered.** Land the `checked_add`
  hardening in-tree quietly (already done); hold the public patch.

(These are research/fold rows, not part of this carry pass — recorded here so the browser unit and
the fold pass both see the gate.)

### Security-sensitive rows

**sec:yes (16, guest-triggerable host DoS / exhaustion / memory-corruption unless noted):** 0004
(halfword-MMIO `panic!`), 0008 (cursor-queue `panic!`), 0012 (broadens the 0044 overflow path —
disclosure-gated), 0013 (SET_SCANOUT_BLOB `panic!`), 0014 (readback `unwrap`), 0024 (transfer_read
`panic!`), 0028 (unhandled-PSCI `panic!` — reachable on STOCK upstream), 0035 (dirty-reset id
collision / UAF hardening), 0041 (host address-space exhaustion), 0044 (blob-map overflow, MED
escape-adjacent — **disclosure-gated**), 0045 (balloon FRQ `unwrap`), 0058 (stale vsock muxer threads
write into a freed/realloc'd guest RX ring — memory corruption + 100% CPU), 0094 (guest vCPU-offline
wedges VMM at ~546% CPU), 0101 (hostile-ring TD DoS guard), 0117 (never-signaling guest fence wedges
the DRM device for every later session), 0125 (console port-reopen VMM abort, guest-triggerable via
`systemctl restart spice-vdagentd`).

**sec:low (3):** 0006 (virtio-mmio spec-enforcement leniency, bounded by max_size), 0027 (DIAG:
guest framebuffer dumped to a world-adjacent /tmp path + latency-injection hook), 0030 (unconditional
error-masking on missing ctx/resource — this pass records it `yes:error-masking(low)`).

### DIAG rows

The 7 pre-flagged DIAG-column rows (carry /tmp/limina-* probe hooks; strip before upstreaming):
0017, 0025, 0027, 0032, 0110, 0112, 0113. Of these, 0017/0032/0110 are carry rows filled this pass
(`fold=strip-diag-first`); 0025/0027 are research rows and 0112/0113 are fold rows (left untouched).
Three further carry rows are DIAG-*class* (env-gated, NO /tmp hook) and are marked
`fold=strip-diag-first` without a DIAG column flag: 0071 (LIMINA_GPU_TRACE), 0092 (LIMINA_WAKE_TRACE),
0106 (LIMINA_WAKE_PROBE).

### The 2026-07-04 triage is stale — predates a third of the series

`docs/upstreaming/00-obvious-fixes-and-security.md` counted **43** libkrun patches; the series is now
126 and its obvious-fixes shortlist predates **0095–0126 entirely**. Two of the strongest send-now
candidates in the current series are guest-visible, upstream-shared bugs it never saw:
**0090** (virtio-fs reports used-ring length 0 → breaks ALL virtiofs shares on Linux ≥7.1 —
`virtio_fs_verify_response` → -EIO → FUSE_INIT fails) and **0125** (console port reopen `expect()` →
guest-triggerable VMM abort, same class as its flagship 0028). Both belong at the front of the
libkrun engagement alongside 0028.

